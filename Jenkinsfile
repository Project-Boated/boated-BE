pipeline {
    agent any
    stages {
        stage('Test') {
            echo 'Test'
            sh 'echo $(pwd)'
        }
        stage('Build') {
            echo 'Build'
        }
        stage('Deploy') {
            echo 'Deploy'
        }
    }
}